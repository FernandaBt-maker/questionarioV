{
  "name": "formulario",
  "version": "1.0.0",
  "description": "Formulário estático com API serverless na Vercel",
  "private": true,
  "scripts": {
    "start": "serve . -l 3000",
    "dev": "npx vercel dev",
    "preview": "serve . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
